import React, { Component } from 'react';
import { View,Text } from 'react-native';

class BottomNav extends Component {
    render() {
        return(
            <View style={{flex:1, backgroundColor: 'blue', width: '100%', flexDirection:'row'}}>
                <View></View>
            </View>
        )
    }
}